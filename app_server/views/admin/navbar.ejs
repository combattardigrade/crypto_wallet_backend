<nav class="navbar">
    <a href="#" class="sidebar-toggler">
        <i data-feather="menu"></i>
    </a>
    <div class="navbar-content">
        <!-- <form class="search-form">
            <div class="input-group">
                <div class="input-group-prepend">
                    <div class="input-group-text">
                        <i data-feather="search"></i>
                    </div>
                </div>
                <input type="text" class="form-control" id="navbarForm" placeholder="Search here...">
            </div>
        </form> -->
        <ul class="navbar-nav">
            
                       
           
            <li class="nav-item dropdown nav-profile">
                <a class="nav-link dropdown-toggle" href="#" id="profileDropdown" role="button" data-toggle="dropdown"
                    aria-haspopup="true" aria-expanded="false">
                    <img src="<%= host %>assets/images/avatar.png" alt="userr">
                </a>
                <div class="dropdown-menu" aria-labelledby="profileDropdown">
                    <div class="dropdown-header d-flex flex-column align-items-center">
                        <div class="figure mb-3">
                            <img src="<%= host %>assets/images/avatar.png" alt="">
                        </div>
                        <div class="info text-center">
                            <p id="navbarUsername" class="name font-weight-bold mb-0"></p>
                            <p id="navbarName" class="email text-muted mb-3"></p>
                        </div>
                    </div>
                    <div class="dropdown-body">
                        <ul class="profile-nav p-0 pt-3">
                            <li class="nav-item">
                                <a href="<%= host %>admin/account" class="nav-link">
                                    <i data-feather="user"></i>
                                    <span>Cuenta</span>
                                </a>
                            </li>      
                            <li class="nav-item">
                                <a href="<%= host %>admin/company" class="nav-link">
                                    <i data-feather="settings"></i>
                                    <span>Empresa</span>
                                </a>
                            </li>                        
                            
                            <li class="nav-item">
                                <a href="<%= host %>admin/logout" class="nav-link">
                                    <i data-feather="log-out"></i>
                                    <span>Cerrar sesión</span>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</nav>

<script>
    (function() {
        const URL = `<%= host %>admin/getAdminData`;
        fetch(URL, {
            method: 'GET',
        })
            .then(data => data.json())
            .then(res => {
                if(res.status == 'OK') {
                    document.getElementById('navbarUsername').innerHTML = res.payload.username
                    document.getElementById('navbarName').innerHTML = res.payload.name
                }
            })
    })()
</script>